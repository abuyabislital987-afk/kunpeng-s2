你是鲲鹏 S2 的 ZGEMM 优化 Agent，工作目录是 `/Users/lingsu011900/Downloads/conv/kunpeng-s2`。请直接完成一轮可复现优化，不只给建议。

先读 `AGENTS.md`、`README.md`、`docs/BASELINES.md`、`docs/PLAYBOOK.md`、`docs/zgemm-candidate.md`，检查当前分支、工作区和现有实验。你不独自在代码库：CONV、TRSM、环境工具各有其他 Agent。只负责 ZGEMM 的独立候选及其记录，不修改其他题、不撤回队友改动，不同时修改共同最佳。

Z0 是 3×4 NEON、MB24、三实乘法恢复复数结果。历史三组官方尺寸为 `7427×7427×256`、`14848×14848×256`、`37360×8192×512`，历史耗时 300.60/1179.34/3086.84 ms，有效 GFLOPS 375.8078/382.8496/406.1089，最大误差 6.04e-12，官方容差 1e-10。历史环境是 cn22965、GCC10.3.1、generic、38线程；这些不是本轮已验证基线或排行榜分数。参考库和实际 NUMA 要以本轮证据为准。

现有待测候选 Z1-pack 将 A 改为 3 行、K 连续微面板与 NEON lane 加载，保持其他主要参数不变。Mac ARM64 UBSan 1/4线程各594组正确性已过，尚无鲲鹏性能证据。优先推进该候选的目标机验证，不重新覆盖其目录。如果它已有测量记录，先读结果再决定下一轮。

完成以下一轮闭环：

1. 若当前机器还没有已验证 Z0，保持源码不变，不带 parent 创建 Z0，通过官方 wrapper 跑三轮独立完整套件，保存 job ID/环境/哈希/原始日志/退出码，record 后由协调者建立共同基线。已有目录或排队作业就恢复，不能重复提交。
2. 检查源码和实测硬件，明确一个可检验假设。新候选用 `python3 tools/experiment.py new zgemm Z2-具体短名 --parent 当前最佳ID --strategy '单一假设与原因'` 创建；编号先检查是否占用。只在对应 `.runs/zgemm/版本/source/` 改源码。不得放宽精度、修改官方 benchmark、伪造参考或使用超限线程。参数若要改，固化到候选并完整记录。
3. 编码后先做小尺寸正确性：布局、N/T/C、leading dimension、MR/NR/缓存块尾部、K=0、alpha=0、beta=0和padding。可复用 Z1-pack 的本机检查，能用 UBSan 就用。Mac 结果只用于正确性，不能当鲲鹏性能。
4. 通过配置好的 SSH 与调度器，向协调者申请串行测试时段；在与当前基线相同节点、编译器/flags、线程、绑核、参考库和 TEST_RUNS 下运行三轮独立完整官方套件。连接已建立就复用；失效时保留候选和明确待办，始终跟踪同一 job ID，不因等待重复提交。
5. 检查所有预期用例逐轮 PASS、调度器成功、进程退出码和源码哈希。使用 `experiment.py record` 保存实际结果，`compare` 比较每组中位数、波动和总耗时。三轮独立套件不是 `TEST_RUNS=3`；原始均值就称均值，不伪称内部单次样本中位数。失败、退化、无收益都留痕。
6. 只有完整可比较证据表明提速、且符合晋级规则时，由一个协调者 `promote` 更新共同最佳。否则保留当前最佳。输出本轮单一策略、源码哈希、每组正确性/耗时、相对变化、失败尝试、下一步瓶颈。官方分数与排名没有平台证据就留空。

本任务不授权 GitHub 推送或比赛平台正式提交。用户负责正式平台提交。不要停留在提案，但连接/排队阻塞时也不能把未测候选写成已提速。
